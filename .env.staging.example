# =============================================================================
# Environment Variables - Staging Environment
# =============================================================================
#
# INSTRUCCIONES:
# 1. Copiar este archivo a .env.staging
# 2. Configurar todos los valores marcados como REQUIRED
# 3. Generar secretos seguros para JWT_SECRET y ENCRYPTION_KEY
#
# Comando para generar secretos:
#   openssl rand -base64 48
#
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (REQUIRED)
# -----------------------------------------------------------------------------
DB_USER=nomina_staging
DB_PASSWORD=secure-staging-password-change-me
DB_NAME=nomina_staging_db

# -----------------------------------------------------------------------------
# SECURITY (REQUIRED)
# -----------------------------------------------------------------------------
# Generar con: openssl rand -base64 48
JWT_SECRET=generate-secure-key-min-32-characters-here
JWT_EXPIRES_IN=24h

# Generar con: openssl rand -base64 48
ENCRYPTION_KEY=generate-secure-key-min-32-characters-here

# -----------------------------------------------------------------------------
# REDIS (REQUIRED)
# -----------------------------------------------------------------------------
REDIS_PASSWORD=secure-redis-password-change-me
REDIS_DB=0

# -----------------------------------------------------------------------------
# APPLICATION
# -----------------------------------------------------------------------------
# URL del frontend (para CORS y redirecciones)
FRONTEND_URL=https://staging.nomina.example.com
VITE_API_URL=/api

# Puertos de Nginx
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# -----------------------------------------------------------------------------
# PAC - PROVEEDOR DE TIMBRADO CFDI
# -----------------------------------------------------------------------------
# Modo: sandbox (pruebas), production (real), disabled
PAC_MODE=sandbox

# Credenciales PAC (solo si PAC_MODE=production)
PAC_PROVIDER=
PAC_USER=
PAC_PASSWORD=

# -----------------------------------------------------------------------------
# AZURE AD / SSO (OPTIONAL)
# -----------------------------------------------------------------------------
AZURE_AD_CLIENT_ID=
AZURE_AD_TENANT_ID=
AZURE_AD_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# EMAIL / SMTP (OPTIONAL)
# -----------------------------------------------------------------------------
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@example.com
SMTP_FROM_NAME=Sistema de Nomina

# -----------------------------------------------------------------------------
# ADVANCED
# -----------------------------------------------------------------------------
# Modo de timbrado CFDI: 'sync' (bloqueante) o 'async' (cola)
# En staging usar 'async' para simular comportamiento de producción
CFDI_STAMP_MODE=async

# Concurrencia de workers para jobs en segundo plano
WORKER_CONCURRENCY=3
