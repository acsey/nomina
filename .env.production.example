# =============================================================================
# Environment Variables - Production Environment
# =============================================================================
#
# ⚠️  IMPORTANTE: PRODUCCIÓN
#
# 1. Copiar este archivo a .env.production
# 2. Configurar TODOS los valores - no dejar valores por defecto
# 3. Generar secretos seguros con: openssl rand -base64 48
# 4. Nunca compartir este archivo ni commitearlo a git
#
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (REQUIRED)
# -----------------------------------------------------------------------------
DB_USER=nomina_prod
DB_PASSWORD=CAMBIAR_CONTRASEÑA_SEGURA_AQUI
DB_NAME=nomina_prod_db

# -----------------------------------------------------------------------------
# SECURITY (REQUIRED - GENERAR VALORES SEGUROS)
# -----------------------------------------------------------------------------
# Generar con: openssl rand -base64 48
JWT_SECRET=GENERAR_CON_openssl_rand_-base64_48
JWT_EXPIRES_IN=8h

# Generar con: openssl rand -base64 48
ENCRYPTION_KEY=GENERAR_CON_openssl_rand_-base64_48

# -----------------------------------------------------------------------------
# REDIS (REQUIRED)
# -----------------------------------------------------------------------------
# Generar con: openssl rand -base64 32
REDIS_PASSWORD=GENERAR_CON_openssl_rand_-base64_32
REDIS_DB=0

# -----------------------------------------------------------------------------
# APPLICATION (REQUIRED)
# -----------------------------------------------------------------------------
# URL pública del sistema (para CORS y redirecciones)
FRONTEND_URL=https://nomina.empresa.com
VITE_API_URL=/api

# Puertos de Nginx (normalmente 80 y 443)
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# -----------------------------------------------------------------------------
# PAC - PROVEEDOR DE TIMBRADO CFDI (REQUIRED FOR CFDI)
# -----------------------------------------------------------------------------
# Modo: production (CFDIs reales al SAT)
PAC_MODE=production

# Proveedor PAC: finkok, sw, etc.
PAC_PROVIDER=finkok
PAC_USER=RFC_EMPRESA
PAC_PASSWORD=CONTRASEÑA_PAC

# -----------------------------------------------------------------------------
# AZURE AD / SSO (OPTIONAL)
# -----------------------------------------------------------------------------
AZURE_AD_CLIENT_ID=
AZURE_AD_TENANT_ID=
AZURE_AD_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# EMAIL / SMTP (RECOMMENDED)
# -----------------------------------------------------------------------------
SMTP_HOST=smtp.empresa.com
SMTP_PORT=587
SMTP_USER=notificaciones@empresa.com
SMTP_PASSWORD=CONTRASEÑA_SMTP
SMTP_FROM_EMAIL=nomina@empresa.com
SMTP_FROM_NAME=Sistema de Nomina

# -----------------------------------------------------------------------------
# WORKERS
# -----------------------------------------------------------------------------
# Número de workers paralelos para procesamiento
WORKER_CONCURRENCY=5
